import { Historic } from '../../interfaces'

const historic: Array<Historic> = []

const initialScoreState = {
    historic
}

const reducer = (state = initialScoreState, action: any) => {
    return state
}

export default reducer
